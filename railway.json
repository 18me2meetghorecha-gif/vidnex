{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'cd backend_app && python manage.py migrate --noinput && python setup_admin.py && gunicorn vidnex_platform.wsgi --bind 0.0.0.0:$PORT --log-file -'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
